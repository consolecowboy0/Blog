---
title: "Hello World"
date: "2026-03-10"
description: "First post. Getting things set up."
---

This is the first post on the blog. Everything here is built with [Astro](https://astro.build), a fast static site generator.

## What this blog supports

- **Markdown** posts - just drop a `.md` file in `src/content/posts/`
- **MDX** posts - use `.mdx` to embed interactive components like charts
- **Images** - drop PNGs/JPGs in `public/images/` and reference them in your posts
- **Code blocks** - syntax highlighted by default

Here's an example code block:

```python
def greet(name: str) -> str:
    return f"Hello, {name}."
```

## Adding images

Place images in `public/images/` and reference them like this:

```markdown
![Alt text](/images/your-image.png)
```

That's it. Simple.
