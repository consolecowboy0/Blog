{
  "name": "legion-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Out-of-process worker that cranks Legion conversation rounds so a session keeps running after the browser tab closes.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1"
  }
}
